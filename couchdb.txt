{
   "all": {
       "map": "function(doc) {\n  emit(doc._id, doc);\n}"
   },
   "users": {
       "map": "function(doc) {\n  if(doc._id == 'users'){\n    for(var u in doc){\n        emit(u, doc[u]);\n    }\n  }\n}"
   },
   "word_lib": {
       "map": "function(doc) {\n  if(doc._id == 'word_lib'){\n    for(var u in doc){\n        emit(u, doc[u]);\n    }\n  }\n}"
   }
}